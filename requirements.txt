# Core data + numerics
pandas>=2.2
numpy>=1.26
pyarrow>=15            # Parquet history store

# HTTP + RSS
requests>=2.31
feedparser>=6.0

# Reporting + config
Jinja2>=3.1
python-dotenv>=1.0

# Optional: benchmark index + global overnight cues + fast history bootstrap.
# The core EOD screener works without it (data comes from the NSE bhavcopy).
yfinance>=0.2.40
